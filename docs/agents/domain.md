# Domain Documentation Layout

## Context

Single-context project. One `CONTEXT.md` at the project root (when created) describes the domain.

## ADRs

Architecture Decision Records live in `docs/adr/` when created.

## Consumer Rules

- Always read `CONTEXT.md` before working on a new area
- Respect existing ADRs when making architectural changes
- Update or create ADRs for significant decisions